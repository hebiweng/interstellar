[build-system]
requires = ["hatchling==1.29.0"]
build-backend = "hatchling.build"

[project]
name = "interstellar-sdk"
version = "0.1.0"
description = "Generated V1 client for the Interstellar professional astrology API"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["interstellar_sdk"]
